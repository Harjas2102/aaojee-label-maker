# Aaojee Label Maker — Python dependencies
# Install with:  pip install -r requirements.txt

# Image generation and label rendering
Pillow>=9.0.0

# Windows 11 look for the program window (optional: falls back to the standard theme)
sv-ttk>=2.6

# Windows printing and printer enumeration (Windows only)
pywin32>=305; sys_platform == "win32"

# Optional: Access .mdb importer (requires Microsoft Access ODBC driver)
pyodbc>=4.0.0; sys_platform == "win32"

# Packaging to .exe (run once during the build step, not needed at runtime)
pyinstaller>=5.0
